import { Card } from '../../components/Card'
import { EmptyState } from '../../components/EmptyState'

/** Placeholder — implemented in Phase 3. */
export function RiderHomePage() {
  return (
    <Card padded={false}>
      <EmptyState
        title="Request a ride"
        description="This screen lands in Phase 3."
      />
    </Card>
  )
}
