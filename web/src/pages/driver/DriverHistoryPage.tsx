import { Card } from '../../components/Card'
import { EmptyState } from '../../components/EmptyState'

/** Placeholder — implemented in Phase 5. */
export function DriverHistoryPage() {
  return (
    <Card padded={false}>
      <EmptyState title="Your trips" description="This screen lands in Phase 5." />
    </Card>
  )
}
